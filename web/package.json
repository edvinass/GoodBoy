{
  "name": "goodboy-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "sh -c 'if [ -d ../goodboy ]; then ../scripts/release-web-tar.sh; elif [ -f public/goodboy.tar.gz ] && [ -f install.sh ]; then :; else echo \"error: run ../scripts/release-web-tar.sh from repo root\" >&2; exit 1; fi'",
    "dev": "npm run prebuild && vite",
    "build": "npm run prebuild && vite build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "vue": "^3.5.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
