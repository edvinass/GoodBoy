{
  "name": "neo-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "sh -c 'if [ -f public/neo.tar.gz ] && [ -f install.sh ]; then :; else echo \"error: missing public/neo.tar.gz or install.sh — run npm run release (or ../scripts/release-web-tar.sh)\" >&2; exit 1; fi'",
    "release": "../scripts/release-web-tar.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "vue": "^3.5.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
